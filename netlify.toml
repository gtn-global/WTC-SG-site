[build]
  # 生成 sitemap 后再发布
  command = "node generate-sitemap.js"
  # 发布目录为仓库根（HTML/CSS/JS/图片都在根目录）
  publish = "."

# 默认入口页（用户访问根路径时打开中文 Deck）
[[redirects]]
  from = "/"
  to = "/WTC-SG-Deck-CN.html"
  status = 200
